表达式语法校验通过。

发现以下 BASE 特征：
- user_score

请从前台复制以上 BASE 特征的当前配置。
请保留前台已有字段，不要先手工补充或改写。

回复样例：

```json
{
  "features": [
    {
      "name": "user_score",
      "raw_name": "user_score",
      "catalog": "/business/current/path"
    }
  ]
}
```
